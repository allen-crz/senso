# Senso API Environment Configuration
# Utility Monitoring PWA with AI-powered meter reading and cost forecasting

# ============================================
# REQUIRED - Application won't start without these
# ============================================

# Supabase Configuration (REQUIRED)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# ============================================
# OPTIONAL - Has defaults, override if needed
# ============================================

# Logging Configuration
LOG_LEVEL=WARNING  # Options: DEBUG, INFO, WARNING, ERROR
# Production: Use WARNING or ERROR for minimal logs (90%+ reduction)
# Development: Use INFO for balanced logging
# Debug: Use DEBUG for verbose logging

# Server Configuration
PORT=8000  # Server port (Render will override with $PORT)

# API Settings
API_V1_STR=/api/v1
PROJECT_NAME=Senso API
VERSION=1.0.0

# CORS Origins (comma-separated)
# Add your frontend URL after deployment
ALLOWED_HOSTS=http://localhost:3000,http://localhost:5173,https://your-frontend.onrender.com

# ============================================
# ML Model Settings (Used by the app)
# ============================================
MODEL_VERSION=v1.0.0
# YOLO models for meter digit detection (must exist in ./models/ directory)
YOLO_MODEL_PATH=./models/electric_meter.pt
WATER_YOLO_MODEL_PATH=./models/water_meter.pt
# Anomaly detection sensitivity (0.05 = 5% outliers expected)
ISOLATION_FOREST_CONTAMINATION=0.1
# Historical data window for ML training
TRAINING_WINDOW_DAYS=30

# External YOLO API (Hugging Face Space) - RECOMMENDED FOR FREE TIER
# Leave empty to use local YOLO models (requires 100MB models + more RAM)
# Set to your HF Space URL to offload YOLO processing (saves ~400MB RAM)
YOLO_API_URL=https://your-username-senso-yolo.hf.space
YOLO_API_TIMEOUT=120

# ============================================
# File Upload Settings
# ============================================
MAX_FILE_SIZE=10485760  # 10MB in bytes
ALLOWED_IMAGE_TYPES=image/jpeg,image/png,image/jpg
UPLOAD_DIR=./uploads

# ============================================
# OPTIONAL FEATURES (Not required for core functionality)
# ============================================

# Database URL (Optional - Supabase client handles all DB operations)
# DATABASE_URL=postgresql://user:password@localhost:5432/senso

# Redis (Optional - not currently used)
# REDIS_URL=redis://localhost:6379

# JWT Secret (Optional - Supabase handles auth)
# SUPABASE_JWT_SECRET=your_jwt_secret

# Email Notifications (Optional - disabled by default)
ENABLE_NOTIFICATIONS=false
EMAIL_ENABLED=false
# SMTP_TLS=true
# SMTP_PORT=587
# SMTP_HOST=smtp.gmail.com
# SMTP_USER=your_email@gmail.com
# SMTP_PASSWORD=your_password